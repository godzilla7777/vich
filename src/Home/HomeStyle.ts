import styled from '@emotion/styled'
import * as E from '../websiteConfig'

export const HomeMainContainer = styled("div")`
  label: HomeMainContainer;
  padding: 10px;
  color: ${E.color.text};
  font-size: ${E.fontSize.header};
  background: ${E.color.secondary}
`;

